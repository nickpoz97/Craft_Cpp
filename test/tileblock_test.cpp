//
// Created by ultimatenick on 27/09/21.
//
#include "catch.hpp"
#include "Geometry/TileBlock.hpp"

/*TEST_CASE("Tileblock instantiation"){
    int i = -1;
    BlockType w;
    do{
        w = static_cast<BlockType>(++i);
        TileBlock t{w};
        REQUIRE(t.getIndex() == w);
        for(TileCube tile_cube : TileBlock::tiles){
            for(int tile_index = 0 ; tile_index < tile_cube.count_visible_faces())
        }
    }
    while(w != BlockType::COLOR_31);
}*/
